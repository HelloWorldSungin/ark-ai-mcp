---
name: mcp-SERVERNAME
description: "One-line of what this does. Use for: 'trigger phrase 1', 'trigger phrase 2', 'trigger phrase 3'. Auth note if needed."
allowed-tools: [Bash]
---

<!-- DESCRIPTION GUIDELINES:
     - Start with what the skill does (verb phrase)
     - Include 2-4 trigger phrases after "Use for:" — these are natural-language
       phrases a user might say that should activate this skill
     - Add auth requirements at the end if applicable
     - Keep under 200 chars for the frontmatter description

     Examples:
       "Call GitHub MCP tools via mcporter CLI — PRs, issues, repos. Use for: 'search code across repos', 'read files from another branch'. Requires GITHUB_TOKEN."
       "Look up library docs via Context7 MCP. Use for: 'how do I use React hooks', 'FastAPI middleware docs', 'pandas merge examples'."
-->

<objective>
What this skill does in 1-2 sentences. Include the server name and what domain it covers.

**When to use this vs alternatives**: Briefly explain when to use this skill vs other tools (gh CLI, Claude-in-Chrome, other MCP skills, etc). This helps Claude pick the right tool.
</objective>

<process>

## Call Syntax

```bash
# Colon-delimited (shell-friendly, best for simple args)
mcporter call SERVERNAME.TOOL_NAME key:value --output json

# Function-call style (better for complex/quoted args)
mcporter call 'SERVERNAME.TOOL_NAME(key: "value", key2: "value2")' --output json
```

Always use `--output json` for machine-readable results.

## Tool Catalog

<!-- PROGRESSIVE DISCLOSURE:
     For servers with many tools (>15-20), use a summary table here and
     extract the full catalog to references/tool-catalog.md:

     | Category | Count | Key Tools |
     |----------|-------|-----------|
     | Category A | 8 | `tool1`, `tool2`, `tool3` |
     | Category B | 5 | `tool4`, `tool5` |

     **Full parameter details**: Read `references/tool-catalog.md`

     For servers with fewer tools (<15), inline the full catalog:
-->

### Category Name (N tools)

| Tool | Key Parameters | Description |
|------|---------------|-------------|
| `tool_name` | `param1`, `param2` | What it does |

### Another Category (N tools)

| Tool | Key Parameters | Description |
|------|---------------|-------------|
| `another_tool` | `param1` | What it does |

## Workflows

<!-- Include 2-4 concrete multi-step workflows showing how to chain tools
     for real tasks. These are the most valuable part of the skill.
     Always keep workflows inline — never extract them to references. -->

### 1. Workflow Name

```bash
# Step 1: What this does
mcporter call 'SERVERNAME.tool1(param: "value")' --output json

# Step 2: What this does
mcporter call 'SERVERNAME.tool2(param: "value")' --output json
```

### 2. Another Workflow

```bash
mcporter call 'SERVERNAME.tool3(param: "value")' --output json
```

</process>

<tips>
<!-- Include 5-10 practical tips. Focus on:
     - Critical parameters that change tool behavior (like method enums)
     - Pagination patterns
     - Auth requirements
     - Common gotchas
     - Performance notes (cold start, token cost, rate limits) -->
- Discover all tools and parameters: `mcporter list SERVERNAME --all-parameters`
- **Tip about critical parameter**: Explanation.
- **Pagination**: How pagination works for list/search tools.
- **Auth**: What auth is needed and how to check it's configured.
- Use `--output json` for machine-readable results.
</tips>

<!-- SIZE TARGETS:
     - Small server (2-5 tools): ~60-80 lines, inline everything
     - Medium server (6-15 tools): ~100-130 lines, inline catalog
     - Large server (16+ tools): ~120-140 lines, extract catalog to references/

     The references/ directory pattern:
       skills/mcp-SERVERNAME/
         SKILL.md              <- Main skill (always loaded)
         references/
           tool-catalog.md     <- Full tool tables (loaded on demand)

     Claude will load references/ files when a user asks about a tool
     not covered in the summary table. The escape hatch is always:
       mcporter list SERVERNAME --all-parameters
-->
